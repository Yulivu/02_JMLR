# Scripts

Command-line wrappers and future automation entry points.

Current code is still runnable via Python modules, for example:

```powershell
python -m tensor_crf_jmlr.event_training.formal_validation_runner --seed-count 10
```

Add thin scripts here only when a workflow becomes stable enough to be a public entry point.
